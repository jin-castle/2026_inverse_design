{
  "Samsung_CIS/final_code/samsung_adam_multi_layer_9pp_PEC_freeform_TiO2.py": "import meep as mp\nimport meep.adjoint as mpa\nimport numpy as np\nfrom autograd import tensor_jacobian_product, grad\nfrom autograd import numpy as npa\nfrom matplotlib import pyplot as plt\nimport os\nimport matplotlib.gridspec as gridspec\nfrom autograd.extend import primitive, defvjp\n\n\nmp.verbosity(1)\n\n# scaling & refractive index\num_scale = 1 # 1A = 1 um\n\nAir = mp.Medium(index=1.0)\nSiO2 = mp.Medium(index=1.45) # n=1.4529 @ 0.55um\nTiO2 = mp.Medium(index=2.65) # n=2.6479 @ 0.55um\n# SiN = mp.Medium(index=2.1)\n\n\nbeta_1_list=[0.9]\n\n\nLR_list= [0.1, 0.1]\nnum_layer_list = [1, 1]\nml_unit_list = [2.0, 2.0]\ncur_final_list = [12, 12]\ninterpixel_gap_list = [0.2, 0.2]\npd_size_list = [0.6]\n\n\n\n# for ml_unit, num_layer in zip(ml_unit_list, num_layer_list):\nfor beta_1 in beta_1_list:\n    for num_layer, ml_unit, LR, cur_final, interpixel_gap, pd_size in zip(num_layer_list, ml_unit_list, LR_list, cur_final_list, interpixel_gap_list, pd_size_list):\n\n        directory_name = f\"Base_adam_{round(num_layer, 1)}layer_{round(LR, 3)}_{round(beta_1, 2)}_ml{round(ml_unit, 2)}_fix1_cur{cur_final}_inter{round(interpixel_gap, 2)}_9pp_PEC_SiO2gap_freeform_TiO2_fl0_PD_moni_{pd_size}\"\n        directory = directory_name + '/design'\n        try:\n            if not os.path.exists(directory):\n                os.makedirs(directory)\n                print(\"디렉토리 생성 성공:\", directory)\n            else:\n                print(\"디렉토리 이미 존재합니다:\", directory)\n        except OSError as e:\n            print(\"디렉토리 생성 실패:\", directory, \"| 에러:\", e)\n\n        # 해상도 및 사이설정\n        resolution = int(50)\n        #o_grid = 1/resolution # 40nm\n        ml_thickness_unit = round(ml_unit, 2)\n        ml_thickness_1 = round(num_layer * ml_thickness_unit, 2) # multi layer thickness 400 nm\n        fl_thickness = round(0, 2) # focal layer thickness 800 nm\n\n        Lpml = round(0.4, 3) # PML thickness 400 nm \n        pml_layers = [mp.PML(thickness = Lpml)]\n        Sourcegap = round(0.2, 3) # up pml to Source length 200nm\n        Sourcespace = round(0.2, 3) # Source to design reigon length 5pixel=200nm\n        Monitorgap = round(0.4, 3) # Monitor to down pml 10pixel=400nm\n\n        # 설계 공간\n        pixel_width = round(1.2, 2)\n        design_region_width_x = round(pixel_width - interpixel_gap,2) # 디자인 영역 x 1.2 um\n        design_region_width_y = round(pixel_width - interpixel_gap,2) # 디자인 영역 y 1.2 um\n        design_region_height = round(ml_thickness_1, 2) # + ml_thickness_3 + ml_thickness_4 + ml_thickness_5 # 디자인 영역 높이 z \n\n        # 전체 공간\n        Sx = round(3 * pixel_width, 2)\n        Sy = round(3 * pixel_width, 2)\n        Sz = round(Lpml + Sourcegap + Sourcespace  + design_region_height + fl_thickness + Monitorgap + Lpml, 2)     \n        # print('Sx=', Sx)\n        # print('Sy=', Sy)   \n        # print('Sz=', Sz)\n        cell_size = mp.Vector3(Sx+ 2*Lpml, Sy+ 2*Lpml, Sz)\n\n        # 파장, 주파수 설정\n        wavelengths_pieces = np.linspace(0.4*um_scale, 0.7*um_scale, 31)\n        wavelengths = np.array([\n                wavelengths_pieces[1], wavelengths_pieces[3], wavelengths_pieces[5], wavelengths_pieces[7], wavelengths_pieces[9],\n                wavelengths_pieces[11], wavelengths_pieces[13], wavelengths_pieces[15], wavelengths_pieces[17], wavelengths_pieces[19],\n                wavelengths_pieces[21], wavelengths_pieces[23], wavelengths_pieces[25], wavelengths_pieces[27], wavelengths_pieces[29]\n        ])\n        frequencies = 1/wavelengths\n        nf = len(frequencies) # number of frequencies\n\n        design_region_resolution = int(resolution)\n\n        # fcen_0 = round(1.9 , 2)\n        # fwidth_0 = round(2.5 , 2)\n\n        # src_0 = mp.GaussianSource(frequency=fcen_0, fwidth=fwidth_0, is_integrated=True)\n\n        # source_center = [0, 0, round(Sz / 2 - Lpml - Sourcegap, 2) ] # Source 위치\n        # source_size = mp.Vector3(Sx, Sy, 0)\n\n        # source = [mp.Source(src_0, component=mp.Ex, size=source_size, center=source_center,),\n        #         mp.Source(src_0, component=mp.Ey, size=source_size, center=source_center,)]\n        \n        # source 설정\n        width = 0.4\n\n        fcen_red = 1/(0.65*um_scale)\n        fwidth_red = fcen_red * width\n\n        fcen_green = 1/(0.55*um_scale)\n        fwidth_green = fcen_green * width\n\n        fcen_blue = 1/(0.45*um_scale)\n        fwidth_blue = fcen_blue * width\n\n        beam_x0 = mp.Vector3(0, 0, Sourcespace/2)  # beam focus (relative to source center)\n        beam_kdir = mp.Vector3(0, 0, -1) # beam propagation direction\n        beam_w0 = 0.6  # beam waist radius\n        beam_E0 = mp.Vector3(1, 1, 0)\n\n        src_0 = mp.GaussianSource(frequency=fcen_red, fwidth=fwidth_red, is_integrated=True)\n\n        src_1 = mp.GaussianSource(frequency=fcen_green, fwidth=fwidth_green, is_integrated=True)\n\n        src_2 = mp.GaussianSource(frequency=fcen_blue, fwidth=fwidth_blue, is_integrated=True)\n\n        source_center = [0, 0, round(Sz / 2 - Lpml - Sourcegap, 2) ] # Source 위치\n        source_size = mp.Vector3(Sx, Sy, 0)\n\n        source = [\n            mp.GaussianBeamSou",
  "2023-Corning-AI-Challenge/iteration.py": "'''\n\n### 코드 로직\n\n1. .npz 파일에서 geometry, gradient 값 read\n    1. iter = 1 이라면 geometry 생성 \n2. geometry, gradient 값을 array 저장\n3. geometry 값에 gradient * n 만큼 +\n4. f0, djdu = opt()\n5. f0 값 저장 \n    1. fom.txt 파일 읽어와서 맨 마지막 줄에 값 저장\n6. 업데이트 된 geometry, 업데이트 된 구조의 adjoint gradient를  .npz로 저장\n\n→ 모델에 넣고 값 출력해서 1번부터 반복\n\n\n'''\n\n\"\"\"\n0.1\t1.15\n0.2\t2.29\n0.3\t3.43\n0.4\t4.57\n0.5\t5.71\n0.6\t6.84\n0.7\t7.97\n0.8\t9.09\n0.9\t10.20\n1.0\t11.31\n\"\"\"\n\nimport meep as mp\nimport meep.adjoint as mpa\nimport numpy as np\nfrom autograd import numpy as npa\nfrom matplotlib import pyplot as plt\nimport secrets\nimport torch\nimport torch.nn as nn\nfrom model import *\nfrom utils import *\n\nmp.verbosity(0)\n\nABS_PATH = os.path.abspath(__file__)\nABS_PATH = \"/\".join(ABS_PATH.split('/')[:-1]) # ~~/joon/corning_\n\n\n# Pretrained model 불러오는 코드\ndef define_model(log_path, epoch=500):\n    args = open_args(log_path)\n    device = f\"cuda:{args.device}\"\n    model_path = os.path.join(log_path,  f'{args.model_name}_{epoch}.pt')\n    print(args)\n    if args.model_name == \"VAE\":\n        if args.vae_cat:\n            args.dim[0][-1] = args.dim[0][-1] - args.latent_dim\n        model = VariationalDeepAdjointPredictor(input_dim=args.in_dim, dim=args.dim, latent_dim=args.latent_dim, layer_num=args.layer_num, condition=args.condition_num, p=args.p, concat=args.vae_cat).to(device)\n    elif args.model_name == \"MLP\":\n        model = LinearModel(input_dim=args.in_dim, dim=args.dim, layer_num=args.layer_num, condition=args.condition_num).to(device)\n    elif args.model_name == \"resMLP\":\n        model = SingleResidualLinearModel(input_dim=args.in_dim, dim=args.dim, layer_num=args.layer_num).to(device)\n    elif args.model_name == \"FNO\":\n        model = FNO(indim=args.in_dim, dim=args.dim, layer_num=args.layer_num, condition_num=args.condition_num).to(device)\n    else:\n        model = ResidualFNO(indim=args.in_dim, dim=args.dim, layer_num=args.layer_num, condition_num=args.condition_num).to(device)\n    \n    model = load_model(model_path, model)\n    \n    return model, device, args\n\n\n# 물리적 법칙을 따르는지를 확인하기 위한 함수입니다. FoM의 first order taylor approximation.\ndef get_norm(adj, geo, update_parameter):\n    negative_indices = np.where(geo+adj*update_parameter < 0)    \n    overpos_indices = np.where(geo+adj*update_parameter > 1)    \n    adj[negative_indices] = -geo[negative_indices] / update_parameter\n    adj[overpos_indices] = (1-geo[overpos_indices]) / update_parameter\n    return np.mean(np.abs(adj**2)) * update_parameter\n\n    \n# simulator를 선언하는 함수입니다.\ndef define_simulator():\n    # mp.verbosity(0)\n    # seed = 240\n    # np.random.seed(seed)\n    \n    LC_up = mp.Medium(epsilon=3.5)\n    LC_lb = mp.Medium(epsilon=2.5)\n    Air = mp.Medium(index=1.0)\n    \n    resolution = 20\n    \n    design_region_width = 5\n    design_region_height = 0.5\n    pml_size = 1.0\n\n    Sx = 2 * pml_size + design_region_width\n    Sy = 2 * pml_size + design_region_height + 8\n    cell_size = mp.Vector3(Sx, Sy)\n\n    pml_layers = [mp.PML(pml_size)]\n    Nx = 100\n    Ny = 1\n\n    # Source parameters\n    fcen = 1 / 1.55\n    width = 0.2\n    fwidth = width * fcen\n    source_center = [0, -Sy/2 + 2, 0]\n    source_size = mp.Vector3(Sx, 0, 0)\n    src = mp.GaussianSource(frequency=fcen, fwidth=fwidth, is_integrated=True)\n    source = [mp.Source(src, component=mp.Ez, size=source_size, center=source_center)]\n\n    design_variables = mp.MaterialGrid(mp.Vector3(Nx, Ny), LC_up, LC_lb, grid_type=\"U_MEAN\")\n    design_region = mpa.DesignRegion(\n        design_variables,\n        volume=mp.Volume(\n            center=mp.Vector3(0,-Sy/2 + 3,),\n            size=mp.Vector3(design_region_width, design_region_height, 0),\n        ),\n    )\n\n    geometry = [\n        mp.Block(\n            center=design_region.center, size=design_region.size, material=design_variables\n        )\n    ]\n\n    sim = mp.Simulation(\n        cell_size=cell_size,\n        boundary_layers=pml_layers,\n        geometry=geometry,\n        sources=source,\n        eps_averaging=False,\n        resolution=resolution,\n    )\n    \n    return sim, design_region, fcen\n\n# Meep simulator의 optimizer를 선언하는 함수입니다.\ndef define_opt(sim, fcen, design_region, condition):  \n    design_region_width = 5\n    design_region_height = 0.5\n    pml_size = 1.0\n\n    Sx = 2 * pml_size + design_region_width\n    Sy = 2 * pml_size + design_region_height + 8\n    cell_size = mp.Vector3(Sx, Sy)\n    \n    monitor_position, monitor_size = mp.Vector3(condition, Sy/2 - 2,), mp.Vector3(0.1,0)\n    FourierFields = mpa.FourierFields(sim,mp.Volume(center=monitor_position,size=monitor_size),mp.Ez,yee_grid=True)\n    ob_list = [FourierFields]\n\n    opt = mpa.OptimizationProblem(\n        simulation=sim,\n        objective_functions=[J],\n        objective_arguments=ob_list,\n        design_regions=[design_region],\n        fcen=fcen,\n        df=0,\n        nf=1,\n    )\n    \n    return opt\n\n# Normalize된 adjoint를 원래 값으로 되돌리는 함수\ndef inv_scaler(adj):\n    return adj * (adj_maxval - adj_minval) + adj_minval\n    \n# 각도에 대한 condition에 대해 normalization 하는 함수\ndef minmaxscaler(x",
  "2023-Corning-AI-Challenge/model.py": "#%%\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport math\n\"\"\"\n    Model 구조의 선택권 : 1. Linear, 2. Conv\n    \n    VAE에서의 ... -> Linearity를 고민할 수 있음. -> Manifold mixup in VAE의 latent vector\n        추가 term으로 임의의 2개 label (동일 structure)에 대해 manifold mixup 해버리자. (기대하는 것 : 동일한 structure에 대해서 label의 linear한 변화에 따라 latent vector 또한 linear한 변화 -> linear semantic)\n\"\"\"\n# kld_loss = torch.mean(-0.5 * torch.sum(1 + log_var - mu ** 2 - log_var.exp(), dim = 1), dim = 0)\ndef kldivergence(mu, log_var, target_mu, target_logvar):\n    return torch.mean(-0.5 * torch.sum(1 + log_var - target_logvar - ((mu - target_mu)**2 - log_var.exp())/math.exp(target_logvar), dim=1), dim=0)\n\nclass Encoder1D(nn.Module):\n    def __init__(self, input_dim=11, dim=32, latent_dim=8, layer_num=2, p=0.8):\n        super().__init__()\n        self.layers = nn.ModuleList([])\n        input_layer = nn.Sequential(\n            nn.Linear(input_dim, dim),\n            nn.SiLU()\n        )\n        self.layers.append(input_layer)\n        for ln in range(layer_num-1):\n            self.layers.append(\n                nn.Sequential(\n                    nn.Linear(dim, dim),\n                    nn.SiLU(),\n                    nn.Dropout(p)\n                )\n            )\n        self.mu_layer = nn.Linear(dim, latent_dim)\n        self.log_var_layer = nn.Linear(dim, latent_dim)\n    \n    def forward(self, x, c):\n        inp = torch.concat([x,c[:, None]], dim=1)\n        for l in self.layers:\n            inp = l(inp)\n        mu = self.mu_layer(inp)\n        log_var = self.log_var_layer(inp)\n        return mu, log_var\n\nclass Decoder1D(nn.Module):\n    def __init__(self, latent_dim=8, dim=32, out_dim=10, layer_num=2, p=0.6):\n        super().__init__()\n        self.layers = nn.ModuleList([])\n        in_layer = nn.Sequential(\n            nn.Linear(latent_dim, dim),\n            nn.SiLU()\n        )\n        self.layers.append(in_layer)\n        for ln in range(layer_num-2):\n            self.layers.append(\n                nn.Sequential(\n                    nn.Linear(dim, dim),\n                    nn.SiLU(),\n                    nn.Dropout(p)\n                )\n            )\n        self.out_layer = nn.Sequential(\n            nn.Linear(dim, out_dim)\n        ) # sigmoid를 쓸 것인가 ? 고민..\n    \n    def forward(self, x):\n        for l in self.layers:\n            x = l(x)\n        return self.out_layer(x)\n        \n\nclass VAE1D(nn.Module): # Assume : input의 구조에 따라 conditional factor가 달라질 수 있다. -> VAE의 input은 conditional factor + structure 이다.\n    def __init__(self, input_dim=11, out_dim=10, dim=32, latent_dim=8, layer_num=4, p=0.6): # input_dim = in_dim + cond_num\n        super().__init__()\n        self.encoder = Encoder1D(input_dim=input_dim, dim=dim, latent_dim=latent_dim, layer_num = layer_num//2, p=p)\n        self.decoder = Decoder1D(latent_dim=latent_dim, dim=dim, out_dim=out_dim, layer_num=layer_num//2, p=p)\n    \n    \n    def encode(self, x, c):\n        mu, log_var = self.encoder(x, c)\n        return [mu, log_var]\n    \n    def decode(self, z):\n        x = self.decoder(z)\n        return x\n    \n    def reparameterize(self, mu, log_var):\n        std = torch.exp(0.5 * log_var)\n        eps = torch.randn_like(std)\n        return eps * std + mu\n    \n    def forward(self, x, c):\n        mu, log_var = self.encode(x, c)\n        z = self.reparameterize(mu, log_var)\n        result = self.decode(z)\n        return result, x, c, mu, log_var, z\n    \n    \n    def loss_function(self, *args): # mixup을 고려할 것인가?\n        recons = args[0]\n        input = args[1]\n        mu = args[2]\n        log_var = args[3]\n\n        kld_weight = args[4] # Account for the minibatch samples from the dataset\n        target_var = args[5]\n        recons_loss = F.mse_loss(recons, input)\n\n        kld_loss = kldivergence(mu, log_var, 0, math.log(target_var))\n        # kld_loss = torch.mean(-0.5 * torch.sum(1 + log_var - mu ** 2 - log_var.exp(), dim = 1), dim = 0)\n\n        loss = recons_loss + kld_weight * kld_loss\n        return {'loss': loss, 'Reconstruction_Loss':recons_loss.detach(), 'KLD':-kld_loss.detach()}\n    \n    def latent_sampling(self, x, c):\n        mu, log_var = self.encode(x, c)\n        z = self.reparameterize(mu, log_var)\n        return z\n\nclass Predictor(nn.Module):\n    def __init__(self, input_dim=10, latent_dim=8, dim=[32, 16], condition=1, p=0.6, residual=False, bn=True, concat=False):\n        super().__init__()\n        self.residual = residual\n        self.condition = condition\n        output_dim = input_dim\n        # if self.condition > 0:\n        #     input_dim += self.condition\n        #     output_dim = input_dim - self.condition\n        self.enc = nn.ModuleList([])\n        self.dec = nn.ModuleList([])\n        input_layer = nn.Sequential(\n            nn.Linear(input_dim, dim[0]),\n            nn.SiLU()\n        )\n        self.enc.append(input_layer)\n        if concat==False:\n            dim[-1] = latent_dim\n        \n        for i in range(1, len(dim)):\n            if bn:\n                self.enc",
  "LNOI-KIST/Mode-converter/Optimization.py": "# Find TE20 mode and LN waveguide validation \n# # LN Straight Waveguide Simulation\n\n# ## 1. Simulation Environment\n\nimport scipy\nimport math\nimport numpy as np\nimport meep as mp\nimport meep.adjoint as mpa\nimport autograd.numpy as npa\nfrom autograd import tensor_jacobian_product, grad\nfrom matplotlib import pyplot as plt\nfrom matplotlib.patches import Circle\nimport nlopt\nimport DFT_loader\nimport os\nimport Sub_Mapping\n\ndesign_dir = \"./A/\"\nmp.verbosity(1)\n# 디렉터리가 없으면 생성\nif not os.path.exists(design_dir):\n    os.makedirs(design_dir)\n\nConstrain= True\nModules= True\nOptimization= True\nGet_Target_field= True\n\nWavelength = 0.775\nSym_geo_width = False\nSym_geo_length = False\nMin_s_top= 0.1  # 100 nm\n\nCapsulation= True\nif Capsulation:\n    #############################\n    Geometry_profile= True      #\n    Forward_mode = 2            #= 2(775nm TE00), 3(1550nm TE10)\n    Backward_mode = 8           #= 8(775nm qTE20), 1(1550nm TE00)\n    #############################= e-beam resolution: 6 nm\n    #############################\n    Main_Parameters= True       #\n    Parameter_activation= True  #\n    Monitor_Profile= True       #\n    Source_profile= True        #########################################################################\n    #                        ####  Set main parameters  ####                                            #\n    if Main_Parameters:                                                                                 #-Parameters for Simulation--%\n        resolution= int(50)                                                                             #-Resolution-----  |\n        Mask_thick= 25                                                                                  #-Tickness of Mask |\n        # design region                                                                                 #                      \n        design_region_x = round(0.5, 2)                                                                 #-Design size-|\n        design_region_y = round(3.0 + 2*round(Mask_thick/resolution,2), 2)                              #             |\n        design_region_z = round(3.0 + 2*round(Mask_thick/resolution,2), 2)                              #             |\n        design_region_resolution = int(resolution)                                                      #-------------|\n                                                                                                        #\n        # Hights (x: thickness)                                                                         #-Hights (X)-----|\n        SiO2_h= round(0.7, 2) # SiO2 padding                                                            #                |\n        LNsub_h= round(0.1, 2) # LN substrate                                                           #                |\n        LNwg_h= design_region_x # LN waveguide                                                          #----------------|\n                                                                                                        #\n        # Width (y: width)                                                                              #-Waveguide (Y)--|\n        min_g= round(0.44, 2) # minimum gap                                                             #                |\n                                                                                                        #                |\n        input_w_top= round(0.6, 2)  # top width (input waveguide)                                       #                |\n        input_w_bot= round(input_w_top + min_g, 2)                                                      #                |\n                                                                                                        #                |\n        w_top= round(0.8, 2) # top width (output waveguide)<---------------------------                 #                |\n        w_bot= round(w_top+ min_g, 2) #bottom width (1222 nm)                                           #----------------|\n    #                     ####  Set main components and monitors  ####                                  #\n    if Parameter_activation:                                                                            #--Variables for simulation--%\n        Lpml= round(10.0/ resolution, 2)                                                                #-Tickness of PML|  \n        # Waveguide length (z: length)                                                                  #\n        waveguide_length= round(0.5, 2) # 입출력단 길이                                                  #-Propagation (Z)|\n        pml_2_src= round(0.3, 2)                                                                        #                |\n        mon_2_pml= round(0.3, 2)                                                                        #                |\n        Prop_length= design_region_z                                                                    #----------------|--> 3 to 4\n           ",
  "LNOI-KIST/Mode-converter/Sub_Mapping.py": "import scipy\nimport math\nimport numpy as np\nimport meep as mp\nimport meep.adjoint as mpa\nimport autograd.numpy as npa\n\ndef get_conic_radius(b, eta_e):\n    if (eta_e >= 0.5) and (eta_e < 0.75):\n        return b / (2 * np.sqrt(eta_e - 0.5))\n    elif (eta_e >= 0.75) and (eta_e <= 1):\n        return b / (2 - 2 * np.sqrt(1 - eta_e))\n    else:\n        raise ValueError(\"(eta_e) must be between 0.5 and 1.\")\n#------------------------------------------------------------------\n\"\"\"\n2D to 3D Sub mapping moudule for x-cut LN waveguide's slanted side wall (67 deg)\n\nThe code was optimized for X: heights, Y: width, Z: length (propagation axis)\n\"\"\"\ndef get_reference_layer(#-- Input parameters ---------------------|\n    Symmetry_in_Sim, # Check the symmetry in simulation           |                                            \n    Width_symmetry,  # Width symmetry in geometry                 |\n    Length_symmetry, # Length symmetry in geometry                | \n    #                                                             |\n    DR_width, # Disign region width                               |\n    N_width,  # Number of DR width grids                          |\n    #                                                             |\n    DR_length, # Disign region length                             |\n    N_length,  # Number of DR length grids                        |\n    #                                                             |\n    DR_res, # Design region resolution                            |\n    #                                                             |\n    Min_size_top, # Minimum linwidth of top layer                 |\n    Min_gap, # Minimum gap width of ref layer                     |\n    #                                                             |\n    input_w_top, # Top width of input waveguide                   |\n    w_top, # Top width of output waveguide                        |\n    #                                                             |\n    x, # Input material density array                             |\n    #                                                             |\n    beta, # Binrization parameter                                 |\n    Mask_pixels, # Number of pixels for mask region               |\n    flag: bool = False # Unused for now                           |\n):                                                                #-- Impose MFS and MGS on x ------------------------|\n    eta_Ref = 0.05                                                      # Thresholding point of tanh projection       |\n    Single_pixel= round(1/DR_res,2)                                                          #                        |\n    Size_Ref= Min_size_top                                                                   # Top layer -------------|\n    Gap_Ref= Min_gap                                                                         #                        |\n    MFS_R = get_conic_radius(Min_size_top, 1-eta_Ref)                                        #                        |\n    MGS_R = get_conic_radius(Gap_Ref, 1-eta_Ref)       # Ensure MGS/MFS-----------------------------------------------|\n    if MGS_R > 4*Single_pixel:                                                               #                        |\n        N_MGS= round(MGS_R/(4*Single_pixel))                                                 #                        |\n        Dilation_R= get_conic_radius((Gap_Ref)/(N_MGS), 1-eta_Ref)                           # 3~4 pixels             |\n        Erosion_R= get_conic_radius((Gap_Ref)/(N_MGS), 1-eta_Ref)                            # 3.3~5 pixels           |\n        Half_R1= get_conic_radius((Min_size_top/2), 1-eta_Ref)                               # MFS/3                  |\n    else:                                              # Ensure MGS/MFS-----------------------------------------------|\n        N_MGS= 1                                                                             #                        |\n        Dilation_R = get_conic_radius(Gap_Ref, 1-eta_Ref)                                    #                        |\n        Erosion_R = get_conic_radius(Gap_Ref, 1-eta_Ref)                                     #                        |                \n    Minimum_mask= (Gap_Ref + Min_size_top -(Gap_Ref)/(N_MGS))/2+ MGS_R                       # Minimum size of Mask   |\n    print(f\"Minimum mask pixel size: {round(Minimum_mask/Single_pixel)} pixels\")             #                        |\n    if Mask_pixels <= Minimum_mask/Single_pixel:                                             #                        |\n        Mask_pixels = int(Minimum_mask/Single_pixel)+2                                       #                        |\n        print(f\"Mask size changed to {Mask_pixels} Pixels \")                                 #                        |\n    Mask= True                                                                               #                        |\n    if Mask: #--------------",
  "Adjoint-FNO/iteration.py": "'''\n\n### 코드 로직\n\n1. .npz 파일에서 geometry, gradient 값 read\n    1. iter = 1 이라면 geometry 생성 \n2. geometry, gradient 값을 array 저장\n3. geometry 값에 gradient * n 만큼 +\n4. f0, djdu = opt()\n5. f0 값 저장 \n    1. fom.txt 파일 읽어와서 맨 마지막 줄에 값 저장\n6. 업데이트 된 geometry, 업데이트 된 구조의 adjoint gradient를  .npz로 저장\n\n→ 모델에 넣고 값 출력해서 1번부터 반복\n\n\n'''\n\n\"\"\"\n0.1\t1.15\n0.2\t2.29\n0.3\t3.43\n0.4\t4.57\n0.5\t5.71\n0.6\t6.84\n0.7\t7.97\n0.8\t9.09\n0.9\t10.20\n1.0\t11.31\n\"\"\"\n\nimport meep as mp\nimport meep.adjoint as mpa\nimport numpy as np\nfrom autograd import numpy as npa\nfrom matplotlib import pyplot as plt\nimport secrets\nimport torch\nimport torch.nn as nn\nfrom model import *\nfrom utils import *\nimport time\n\nmp.verbosity(0)\n\nABS_PATH = os.path.abspath(__file__)\nABS_PATH = \"/\".join(ABS_PATH.split('/')[:-1]) \n\n\n# Load pretrained model\ndef define_model(log_path, epoch=500):\n    args = open_args(log_path)\n    device = f\"cuda:0\"\n    model_path = os.path.join(log_path,  f'{args.model_name}_{epoch}.pt')\n    print(args)\n    if args.model_name == \"VAE\":\n        if args.vae_cat:\n            args.dim[0][-1] = args.dim[0][-1] - args.latent_dim\n        model = VariationalDeepAdjointPredictor(input_dim=args.in_dim, dim=args.dim, latent_dim=args.latent_dim, layer_num=args.layer_num, condition=args.condition_num, p=args.p, concat=args.vae_cat).to(device)\n    elif args.model_name == \"MLP\":\n        model = LinearModel(input_dim=args.in_dim, dim=args.dim, layer_num=args.layer_num, condition=args.condition_num, p=args.p, fourier_embed=args.fourier_embedding).to(device)\n    elif args.model_name == \"resMLP\":\n        model = SingleResidualLinearModel(input_dim=args.in_dim, dim=args.dim, layer_num=args.layer_num).to(device)\n    elif args.model_name == \"FNO\":\n        model = FNO(indim=args.in_dim, dim=args.dim, layer_num=args.layer_num, condition_num=args.condition_num).to(device)\n    elif args.model_name == \"NewFNO\":\n        model = NewFNO(indim=args.in_dim, dim=args.dim, mode=args.mode, layer_num=args.layer_num, condition_num=args.condition_num).to(device)\n    else:\n        model = Unet1d(input_dim=1, dim=args.dim, condition=args.condition_num).to(device)\n    \n    model = load_model(model_path, model)\n    model.eval()\n    return model, device, args\n\n\n\n# First order taylor approximation of FoM\ndef get_norm(adj, geo, update_parameter):\n    negative_indices = np.where(geo+adj*update_parameter < 0)    \n    overpos_indices = np.where(geo+adj*update_parameter > 1)    \n    adj[negative_indices] = -geo[negative_indices] / update_parameter\n    adj[overpos_indices] = (1-geo[overpos_indices]) / update_parameter\n    return np.mean(np.abs(adj**2)) * update_parameter\n\n    \n# Define a simulator.\ndef define_simulator():\n    # mp.verbosity(0)\n    # seed = 240\n    # np.random.seed(seed)\n    \n    LC_up = mp.Medium(epsilon=3.5)\n    LC_lb = mp.Medium(epsilon=2.5)\n    Air = mp.Medium(index=1.0)\n    \n    resolution = 20\n    \n    design_region_width = 5\n    design_region_height = 0.5\n    pml_size = 1.0\n\n    Sx = 2 * pml_size + design_region_width\n    Sy = 2 * pml_size + design_region_height + 8\n    cell_size = mp.Vector3(Sx, Sy)\n\n    pml_layers = [mp.PML(pml_size)]\n    Nx = 100\n    Ny = 1\n\n    # Source parameters\n    fcen = 1 / 1.55\n    width = 0.2\n    fwidth = width * fcen\n    source_center = [0, -Sy/2 + 2, 0]\n    source_size = mp.Vector3(Sx, 0, 0)\n    src = mp.GaussianSource(frequency=fcen, fwidth=fwidth, is_integrated=True)\n    source = [mp.Source(src, component=mp.Ez, size=source_size, center=source_center)]\n\n    design_variables = mp.MaterialGrid(mp.Vector3(Nx, Ny), LC_up, LC_lb, grid_type=\"U_MEAN\")\n    design_region = mpa.DesignRegion(\n        design_variables,\n        volume=mp.Volume(\n            center=mp.Vector3(0,-Sy/2 + 3,),\n            size=mp.Vector3(design_region_width, design_region_height, 0),\n        ),\n    )\n\n    geometry = [\n        mp.Block(\n            center=design_region.center, size=design_region.size, material=design_variables\n        )\n    ]\n\n    sim = mp.Simulation(\n        cell_size=cell_size,\n        boundary_layers=pml_layers,\n        geometry=geometry,\n        sources=source,\n        eps_averaging=False,\n        resolution=resolution,\n    )\n    \n    return sim, design_region, fcen\n\n# Optimizer of Meep simlator (FDTD)\ndef define_opt(sim, fcen, design_region, condition):  \n    design_region_width = 5\n    design_region_height = 0.5\n    pml_size = 1.0\n\n    Sx = 2 * pml_size + design_region_width\n    Sy = 2 * pml_size + design_region_height + 8\n    cell_size = mp.Vector3(Sx, Sy)\n    \n    monitor_position, monitor_size = mp.Vector3(condition, Sy/2 - 2,), mp.Vector3(0.1,0)\n    FourierFields = mpa.FourierFields(sim,mp.Volume(center=monitor_position,size=monitor_size),mp.Ez,yee_grid=True)\n    ob_list = [FourierFields]\n\n    opt = mpa.OptimizationProblem(\n        simulation=sim,\n        objective_functions=[J],\n        objective_arguments=ob_list,\n        design_regions=[design_region],\n        fcen=fcen,\n        df=0,\n        nf=1,\n    )\n    \n    return opt\n\ndef inv_scaler(adj):\n    return adj ",
  "surrogate-solver/main.py": "import argparse\n\nfrom config.loader import load_config\nfrom src.design.run import generate_design\nfrom src.simulation.run import run_simulation\nfrom src.visualization.run import visualize_result\nfrom src.utils.helpers import clear_console, menu\n\n\n# ======================\n# MAIN EXECUTION BLOCK\n# ======================\n\n# Parse command line argument\nparser = argparse.ArgumentParser()\nparser.add_argument(\"--config\", default=\"simplex_noise.yaml\")\nargs = parser.parse_args()\n\ncfg = load_config(args.config)\n\noptions = [\n    \"Generate Design & Run Simulation\",\n    \"Generate Design\",\n    \"Run Simulation\",\n    \"Visualize Result\"\n]\n\nactions = {\n    1: [generate_design, run_simulation],\n    2: [generate_design],\n    3: [run_simulation],\n    4: [visualize_result]\n}\n\nwhile True:\n    # Selection Menu\n    sel = menu(options)\n    if sel == 0:\n        break\n    \n    print(f\"{options[sel - 1]}\")\n\n    # Execute user selection\n    funcs = actions.get(sel)\n    if funcs:\n        for func in funcs:\n            func(cfg)\n    else:\n        print(\"Invalid selection\")\n    \n    input(\"\\nPress Enter to return...\")\n    clear_console()\n",
  "Chiral_metasurface/3d_adjoint_chiral_meta.py": "# %%\nimport meep as mp\nimport meep.adjoint as mpa\nimport numpy as np\nfrom autograd import numpy as npa\nfrom autograd import tensor_jacobian_product, grad\nimport nlopt\nfrom matplotlib import pyplot as plt\nfrom matplotlib.patches import Circle\nimport pickle\n\n# %%\nmp.verbosity(1)\num_scale = 1\nseed = 240  # setting the random number seed as 240 (for reproducibility)\nnp.random.seed(seed)  # numpy random seed setting\n\n# Material setting and refractive index setting\ndef def_WS2():\n\n    # default unit length is 1 μm\n    um_scale = 1.0\n\n    # conversion factor for eV to 1/μm [=1/hc]\n    eV_um_scale = um_scale / 1.23984193\n\n    # ------------------------------------------------------------------\n    # WS2 bulk from  https://doi.org/10.1021/acs.nanolett.3c02051\n    # wavelength range: 0.6 - 0.8 μm\n    # Material modeling code is in GitHub\n    WS2_o_eps_infty = 10.5686191135809\n    WS2_o_frq1 = 1.60571176600312\n    WS2_o_gamma1 = 0.0483718190665989\n    WS2_o_sig1 = 0.109917872778838\n    WS2_o_frq2 = 1.45174790445326\n    WS2_o_gamma2 = 0.239930959161920\n    WS2_o_sig2 = 0.0856209686274106\n    WS2_o_frq3 = 1.96136652563532\n    WS2_o_gamma3 = 1.08966976489645e-12\n    WS2_o_sig3 = 3.37987155948019\n    WS2_o_frq4 = 1.58619498393870\n    WS2_o_gamma4 = 0.0329518082904728\n    WS2_o_sig4 = 0.254224343363809\n    WS2_o_frq5 =  1.66231438960124\n    WS2_o_gamma5 = 0.100006254332917\n    WS2_o_sig5 = 0.203902073861584\n    \n    WS2_e_eps_infty = 1\n    WS2_e_frq1 =  3.55003330756111\n    WS2_e_sig1 =  4.78000849186307\n    WS2_e_gamma1 = 0\n\n    WS2_susc = [\n        mp.LorentzianSusceptibility(frequency=WS2_o_frq1, gamma=WS2_o_gamma1, sigma_diag=WS2_o_sig1*mp.Vector3(1,1,0)),\n        mp.LorentzianSusceptibility(frequency=WS2_o_frq2, gamma=WS2_o_gamma2, sigma_diag=WS2_o_sig2*mp.Vector3(1,1,0)),\n        mp.LorentzianSusceptibility(frequency=WS2_o_frq3, gamma=WS2_o_gamma3, sigma_diag=WS2_o_sig3*mp.Vector3(1,1,0)),\n        mp.LorentzianSusceptibility(frequency=WS2_o_frq4, gamma=WS2_o_gamma4, sigma_diag=WS2_o_sig4*mp.Vector3(1,1,0)),\n        mp.LorentzianSusceptibility(frequency=WS2_o_frq5, gamma=WS2_o_gamma5, sigma_diag=WS2_o_sig5*mp.Vector3(1,1,0)),\n        mp.LorentzianSusceptibility(frequency=WS2_e_frq1, gamma=WS2_e_gamma1, sigma_diag=WS2_e_sig1*mp.Vector3(0,0,1)),\n    ]\n\n    WS2_material = mp.Medium(epsilon_diag=mp.Vector3(WS2_o_eps_infty,WS2_o_eps_infty, WS2_e_eps_infty), E_susceptibilities=WS2_susc)\n    return WS2_material\n# Ge = mp.Medium(index=4.2)\nGlass = mp.Medium(index=1.5)\n\nAir = mp.Medium(epsilon_diag=mp.Vector3(1,1,1))\n\nWS2_material = def_WS2()\n\n# simulation space setting\ndesign_region_height = 0.25\ndesign_region_width = 0.38\nlpml = 0.5\nLx = design_region_width\nLy = design_region_width\nLz = 2*lpml + design_region_height + 1.0\ncell_size = mp.Vector3(Lx, Ly, Lz)\nresolution = 100\nsim_time = 40\nfolder_name = \"geometry_400\" #location for saving the results \n\n# wavelength, frequency setting\nwavelength = 0.680\nfrequency = 1/wavelength\nwidth = 0.5\n\n# source setting\nsource_pos = 0.5*Lz - lpml - 1.0/resolution\nsource_center = mp.Vector3(0, 0 , source_pos) # Source position\nsource_size = mp.Vector3(Lx , Ly, 0)\n\ntop_monitor_pos, bot_monitor_pos = +0.5*Lz-lpml-0.1, -0.5*Lz+lpml+0.1\n\n\nsrc = mp.GaussianSource(frequency=frequency, fwidth=frequency*width, is_integrated=True)\n\nsource_L = [mp.Source(src, component=mp.Ex, size=source_size, center=source_center, amplitude = 1),\n          mp.Source(src, component=mp.Ey, size=source_size, center=source_center, amplitude = -1j),\n          ]\nsource_R = [mp.Source(src, component=mp.Ex, size=source_size, center=source_center, amplitude = 1),\n          mp.Source(src, component=mp.Ey, size=source_size, center=source_center, amplitude = 1j),\n          ]\n\n\n\n#디자인 영역 minimum length, penalization parameter setting\nminimum_length = 0.02  # minimum length scale (microns)\neta_i = 0.5  # blueprint (or intermediate) design field thresholding point (between 0 and 1)\neta_e = 0.55  # erosion design field thresholding point (between 0 and 1)\neta_d = 1 - eta_e  # dilation design field thresholding point (between 0 and 1)\nfilter_radius = mpa.get_conic_radius_from_eta_e(minimum_length, eta_e)\ndesign_region_resolution = int(resolution)\n\n# pixels of design region - using the design region resolution and size\nNx = int(round(design_region_width*design_region_resolution)) + 1\nNy = int(round(design_region_width*design_region_resolution)) + 1\nNz = 1\n\n# setting the design region with design material and pixel information\ndesign_variables = mp.MaterialGrid(mp.Vector3(Nx, Ny, Nz), Air, WS2_material, grid_type=\"U_MEAN\")\ndesign_region = mpa.DesignRegion(\n    design_variables,\n    volume=mp.Volume(\n        center=mp.Vector3(0, 0, 0),\n        size=mp.Vector3(design_region_width, design_region_width, design_region_height),\n    ),\n)\n\n\n# using conic_filter \ndef mapping(x, eta, beta):\n    # filter\n    x = x.flatten()\n    filtered_field = mpa.conic_filter(\n        x,\n        filter_radius,\n        design_region_width,\n        design_region_wi",
  "inverse-design-of-ultrathin-metamaterial-absorber/2D-absorber-adjoint-optimization/2D_optimization.py": "import meep as mp\nimport meep.adjoint as mpa\nimport numpy as np\nfrom autograd import numpy as npa\nfrom autograd import tensor_jacobian_product, grad\nimport autograd.numpy as np\nimport nlopt\nfrom matplotlib import pyplot as plt\nfrom matplotlib.patches import Circle\nimport math\nimport os\nimport sys\nimport csv\n\n\noutput_dir = \"results_output\"\ncsv_file_path = os.path.join(output_dir, \"iteration_logs.csv\")\n\nif not os.path.exists(csv_file_path):\n    with open(csv_file_path, mode=\"w\", newline=\"\") as csv_file:\n        csv_writer = csv.writer(csv_file)\n        csv_writer.writerow([\"Iteration\", \"FoM\", \"Binarization Degree\"])  # 헤더 작성\n\nmp.verbosity(0)\n\nmm_scale = 1/20 #20mm scale\n\n''' ==================== input parameter ==================== '''\n###----- resolution setting -----###\nresolution = 160  #pixel/mm & 20mm/0.125mm = 160 --> 1pixel : 0.125mm\ndesign_resolution = resolution\npixel = 1/resolution #1mm = 8pixel\n\n###-----wavelength parameters -----###\nwvl = 2.0 #single wavelength case\nwvl_min = 1.5 # min wavelength (gaussian)\nwvl_max = 3.0 # max wavelength (gaussian)\nfcenter = 1 / wvl\n\n###----- frequencies -----###\nnfreq = 50  \nfmax=1/wvl_min\nfmin=1/wvl_max\nfrequencies = np.linspace(fmin, fmax, nfreq)\ndf=fmax-fmin\nfwidth = df\n\n###-----material properties -----###\nAir = mp.Medium(index=1.0)\nSiO2_eps_r=3.91\nSiO2_eps_i=0.0015\nSiO2_Dcon=2*math.pi*fcenter*mm_scale*1e-3*SiO2_eps_i/SiO2_eps_r\nSiO2= mp.Medium(epsilon=SiO2_eps_r, D_conductivity=SiO2_Dcon)\n\nSi_eps_r=11.9\nSi_eps_i=0.0006\nSi_Dcon=2*math.pi*fcenter*mm_scale*1e-3*Si_eps_i/Si_eps_r\nSi= mp.Medium(epsilon=Si_eps_r, D_conductivity=Si_Dcon)\n\nSiN_eps_r=7.0\nSiN_eps_i=0.0097\nSiN_Dcon=2*math.pi*fcenter*mm_scale*1e-3*SiN_eps_i/SiN_eps_r\nSiN= mp.Medium(epsilon=SiN_eps_r, D_conductivity=SiN_Dcon)\n\nITO_eps_r=0.5e6/5000\nITO_con=0.5e6/4500\nITO_Dcon=(20e-3/3e8)*ITO_con/(ITO_eps_r*8.854e-12)\nITO_model = mp.Medium(epsilon=ITO_eps_r, D_conductivity=ITO_Dcon)\nITO_thick=0.125*mm_scale\n\nPET_eps_r=2.65\nPET_eps_i=0.005\nPET_Dcon=2*math.pi*fcenter*mm_scale*1e-3*PET_eps_i/PET_eps_r\nPET_model= mp.Medium(epsilon=PET_eps_r, D_conductivity=PET_Dcon)\nPET_thick = 0.125*mm_scale\n\nPTFE_eps_r=2.10\nPTFE_eps_i=0.0004\nPTFE_Dcon=2*math.pi*fcenter*mm_scale*1e-3*PET_eps_i/PET_eps_r\nPTFE_model= mp.Medium(epsilon=PTFE_eps_r, D_conductivity=PTFE_Dcon)\n\ndefault_material = Air\ndefault_n = 1.0\n\n###----- geometry parameters -----###\nITO_thick = 0.125 *mm_scale\nPET_thick = 0.125 *mm_scale\npml_thick = 5/resolution\nGND_thick = ITO_thick*10\nGND_gap = 1*mm_scale + 2/resolution\n\ngeo_thick = ITO_thick + GND_gap + GND_thick\nsrc2mat = 12*mm_scale\n\nsrc_space = 2/resolution\nmonitor_space = 2/resolution\n\nsize_Y = 2*pml_thick + src_space + src2mat + geo_thick + monitor_space*2\nsize_X = 5*mm_scale\ncell = mp.Vector3(size_X,size_Y)\n\nfirst_mat_location = -size_Y/2 + pml_thick + src_space + src2mat\n\ndesign_region_width = size_X\ndesign_region_height = GND_gap\ndesign_material_1 = Si\ndesign_material_2 = SiO2\n\npatch_width = 1*mm_scale\npatch_gap = 5*mm_scale\npatch_height = 0.125 *mm_scale\nkpoint = mp.Vector3(0,0,0)\n\n\nrandom_initial = True # True : random initial design field, False : initial design field from previous result\n\n###----- filter parameters -----###\nminimum_length = 0.01  \neta_i = 0.5 \neta_e = 0.55 \neta_d = 1 - eta_e \n\n\n''' ==================== function definition ==================== '''\n###----- filter -----###\nfilter_radius = mpa.get_conic_radius_from_eta_e(minimum_length, eta_e)\n# print(filter_radius)\n\n###----- geometry -----###\npml_layers = [mp.PML(thickness=pml_thick, direction=mp.Y)]\n\n###----- design region -----###\nNx = int(round(design_resolution * design_region_width)) + 1\nNy = int(round(design_resolution * design_region_height)) + 1\nprint(Nx, Ny)\n\nclass Tee:\n    def __init__(self, *files):\n        self.files = files\n\n    def write(self, obj):\n        for f in self.files:\n            f.write(obj)\n            f.flush()\n\n    def flush(self):\n        for f in self.files:\n            f.flush()\n\n''' ==================== Multi design region - Top / bottom / all ==================== '''\ndesign_variables_T = mp.MaterialGrid(mp.Vector3(Nx, Ny), design_material_1, design_material_2, grid_type=\"U_MEAN\")\ndesign_region_top = mpa.DesignRegion(\n    design_variables_T,\n    volume=mp.Volume(\n        center=mp.Vector3(0, first_mat_location +ITO_thick + design_region_height/2 , 0),\n        size=mp.Vector3(patch_width, design_region_height, 0),\n    ),\n)\n\ndesign_variables_B = mp.MaterialGrid(mp.Vector3(Nx, Ny), design_material_1, design_material_2, grid_type=\"U_MEAN\")\ndesign_region_bottom = mpa.DesignRegion(\n    design_variables_B,\n    volume=mp.Volume(\n        center=mp.Vector3(0,first_mat_location-design_region_height/2,0),\n        size=mp.Vector3(patch_width, design_region_height, 0),\n    ),\n)\ndesign_variables_L = mp.MaterialGrid(mp.Vector3(Nx, Ny), design_material_1, design_material_2, grid_type=\"U_MEAN\")\ndesign_region_Left = mpa.DesignRegion(\n    design_variables_L,\n    volume=mp.Volume(\n        center=mp.Vector3(-(size_X/2+pat",
  "2025-Large_scale_metalens/250717_Cylindrical_metalens_reproduce_z_averaging_GLC.py": "import meep as mp \nimport meep.adjoint as mpa\nimport numpy as np\nfrom autograd import numpy as npa\nfrom autograd import tensor_jacobian_product, grad\n\nimport nlopt\nfrom matplotlib import pyplot as plt\n\n\nmp.verbosity(1)\n\n# material 설정\nresin = mp.Medium(index = 1.5228)\nAir = mp.Medium(epsilon=1.0)\n\n\nwvl_r = 0.635 # Red\nwvl_b = 0.45  # Blue\nwvl_g = 0.532  # Green\n\nwavelengths = np.array([wvl_r, wvl_g, wvl_b])\nfrequencies = 1 / wavelengths\nfrequencies_width = 0.1\n\ndesign_region_width = 10 # 실제로는 500으로 맞출 것\ndesign_region_height = 0.7\ndair = 2\n\n# focal length\nna = 0.7\nfocal_length = design_region_width * np.sqrt((1 / (na**2)) - 1)\nprint(\"================== Focal Length ==================\")\nprint(\"Focal Length: \", focal_length)\nprint(\"==================================================\")\n\ndpml = 0.5 * wavelengths[1]  # PML 두께\ndsource = 0.1 # PML 윗부분부터 source까지의 거리\ndsub = 0.1 * wavelengths[1]  # Source부터 substrate까지의 거리\ndpad = 0.5 # Pad 두께\n\nresolution = 20\n\nsr = design_region_width + dpml\nsphi = 0\nsz = dpml + dsource + dsub + design_region_height + dpad + dpml\ncell_size = mp.Vector3(sr, sphi, sz)\n\n\n# PML\npml_layers = [mp.PML(dpml)]\n\n\n\ndesign_region_resolution = int(20)\n\n# source 정의 - Multi-objective problem이라 source를 3 개 정의함\n\nsrc_pt = [sr/2,0,-0.5 *sz + dpml + dsource]\n\nsource_r = [\n    mp.Source(\n        mp.GaussianSource(\n            frequency=frequencies[0], fwidth=frequencies_width, is_integrated=True\n        ),\n        component=mp.Er,\n        center=src_pt,\n        size = mp.Vector3(sr, 0, 0),\n        amplitude=1.0\n    ),\n    mp.Source(\n        mp.GaussianSource(\n            frequency=frequencies[0], fwidth=frequencies_width, is_integrated=True\n        ),\n        component=mp.Ep,\n        center=src_pt,\n        size = mp.Vector3(sr, 0, 0),\n        amplitude=1j\n    ),\n]\n\nsource_g = [\n    mp.Source(\n        mp.GaussianSource(\n            frequency=frequencies[1], fwidth=frequencies_width, is_integrated=True\n        ),\n        component=mp.Er,\n        center=src_pt,\n        size = mp.Vector3(sr, 0, 0),\n        amplitude=1.0\n    ),\n    mp.Source(\n        mp.GaussianSource(\n            frequency=frequencies[1], fwidth=frequencies_width, is_integrated=True\n        ),\n        component=mp.Ep,\n        center=src_pt,\n        size = mp.Vector3(sr, 0, 0),\n        amplitude=1j\n    ),\n]\n\nsource_b = [\n    mp.Source(\n        mp.GaussianSource(\n            frequency=frequencies[2], fwidth=frequencies_width, is_integrated=True\n        ),\n        component=mp.Er,\n        center=src_pt,\n        size = mp.Vector3(sr, 0, 0),\n        amplitude=1.0\n    ),\n    mp.Source(\n        mp.GaussianSource(\n            frequency=frequencies[2], fwidth=frequencies_width, is_integrated=True\n        ),\n        component=mp.Ep,\n        center=src_pt,\n        size = mp.Vector3(sr, 0, 0),\n        amplitude=1j\n    ),\n]\n\n\nNr = int(design_region_width * design_region_resolution + 1)\nNh = int(design_region_height * design_region_resolution + 1)\n\ndesign_variables = mp.MaterialGrid(mp.Vector3(Nr, 0, Nh), Air, resin, grid_type=\"U_MEAN\")\n\ndesign_region = mpa.DesignRegion(\n    design_variables,\n    center=mp.Vector3(design_region_width/2, 0, -0.5*sz + dpml+dsource+dsub+0.5*design_region_height),\n    size=mp.Vector3(design_region_width, 0, design_region_height),\n)\n\ngeometry = [\n    mp.Block(\n        center=design_region.center, size=design_region.size, material=design_variables\n    )\n]\n\n\n\n\nsim_r = mp.Simulation(\n    cell_size=cell_size,\n    resolution=resolution,\n    geometry = geometry,\n    boundary_layers=pml_layers,\n    sources=source_r,\n    default_material=Air,\n    dimensions=mp.CYLINDRICAL,\n    m=1\n)\nsim_g = mp.Simulation(\n    cell_size=cell_size,\n    resolution=resolution,\n    geometry = geometry,\n    boundary_layers=pml_layers,\n    sources=source_g,\n    default_material=Air,\n    dimensions=mp.CYLINDRICAL,\n    m=1\n)\n\nsim_b = mp.Simulation(\n    cell_size=cell_size,\n    resolution=resolution,\n    geometry = geometry,\n    boundary_layers=pml_layers,\n    sources=source_b,\n    default_material=Air,\n    dimensions=mp.CYLINDRICAL,\n    m=1\n)\n\nfar_x = [mp.Vector3(1/resolution, 0, -0.5 *sz + dpml+dsource+dsub+design_region_height+focal_length)]\nNearRegions = [\n    mp.Near2FarRegion(\n        center=mp.Vector3(design_region_width/2, 0, 0.5),\n        size=mp.Vector3(design_region_width, 0,0),\n        weight=+1,\n    )\n]\nFarFields_r = mpa.Near2FarFields(sim_r, NearRegions, far_x)\nFarFields_g = mpa.Near2FarFields(sim_g, NearRegions, far_x)\nFarFields_b = mpa.Near2FarFields(sim_b, NearRegions, far_x)\nob_list = [FarFields_r, FarFields_g, FarFields_b]\n\n\ndef log_intensity(FF):\n    ex_real_part = npa.real(FF)\n    intensity = ex_real_part**2\n    return npa.log(intensity + 1e-20)\n\n\n# Optimization problem\nopt_r = mpa.OptimizationProblem(\n    simulation=sim_r,\n    objective_functions=[log_intensity],\n    objective_arguments=[ob_list[0]],\n    fcen=frequencies[0],\n    df=0,\n    nf=1,\n    design_regions=[design_region],\n    decay_by=1e-1,\n)\n\nopt_g = mpa.OptimizationProblem(\n    sim",
  "shape_optimization/chanik_ver/shapeopt.py": "'''\n3D shape optimization code\n- Chanik Kang\n'''\n\nfrom mpi4py import MPI\nimport sys\n\ncomm = MPI.COMM_WORLD\nrank = comm.Get_rank() \nsize = comm.Get_size()  # 총 프로세서 수를 얻습니다.\n\n### Part 1. Gradient summation ###\ndef coordinate_to_index(x, y, z):\n    return x * 22801 + y * 151 + z\n\ndef read_gradient_from_file(index):\n    # 파일을 열고 해당 인덱스의 값을 읽어 반환\n    if rank == 0:\n        try:\n            with open(\"backup/gradient.txt\", \"r\") as file:\n                for current_line, line in enumerate(file):\n                    if current_line == index:\n                        return float(line.strip())  # 라인의 내용을 정수로 반환\n        except FileNotFoundError:\n            print(\"File not found. Please check the file path and name.\")\n            return None\n        except Exception as e:\n            print(f\"An error occurred: {e}\")\n            return None\n    # if comm.size > 1:\n    #     data = comm.bcast(current_line, root=0)\n    #     data = comm.bcast(line, root=0)\n\n    # 파일의 끝에 도달했을 경우\n    print(\"Specified index exceeds file length.\")\n    return None\n\ndef calculate_gradient_summation_right(y1, z1, flag):\n    y1 = y1 - 1\n    print ('gradient sum left y1 =', y1)\n    # exit(1)\n    if y1 < 0:\n        return 0\n    # 주어진 y1, z1 좌표를 기준으로 계산할 gradient 좌표\n    if flag == True:\n        coordinates = [\n            (0, y1, z1), (1, y1+1, z1), (2, y1 + 1, z1), (3, y1 + 2, z1), (4, y1 + 2, z1),\n            (5, y1 + 2, z1), (6, y1 + 3, z1), (7, y1 + 3, z1), (8, y1 + 4, z1), (9, y1 + 4, z1),\n            (10, y1 + 4, z1), (11, y1 + 5, z1), (12, y1 + 5, z1), (13, y1 + 6, z1), (14, y1 + 6, z1),\n            (15, y1 + 6, z1), (16, y1 + 7, z1), (17, y1 + 7, z1), (18, y1 + 8, z1), (19, y1 + 8, z1),\n            (20, y1 + 8, z1), (21, y1 + 9, z1), (22, y1 + 9, z1), (23, y1 + 10, z1), (24, y1 + 10, z1), (25, y1+10, z1)\n        ]\n        \n    else:\n        coordinates = [\n            (0, y1, z1), (1, y1-1, z1), (2, y1-1, z1), (3, y1-2, z1), (4, y1-2, z1),\n            (5, y1-2, z1), (6, y1-3, z1), (7, y1-3, z1), (8, y1-4, z1), (9, y1-4, z1),\n            (10, y1-4, z1), (11, y1-5, z1), (12, y1-5, z1), (13, y1-6, z1), (14, y1-6, z1),\n            (15, y1-6, z1), (16, y1-7, z1), (17, y1-7, z1), (18, y1-8, z1), (19, y1-8, z1),\n            (20, y1-8, z1), (21, y1-9, z1), (22, y1-9, z1), (23, y1-10, z1), (24, y1-10, z1), (25, y1-10, z1)\n        ]\n\n    indices = [coordinate_to_index(x, y, z) for x, y, z in coordinates]\n    print('index = ', indices)\n\n    # 각 인덱스에 대해 gradient 값을 읽어와 합산합니다.\n    total_gradient = sum(read_gradient_from_file(index) for index in indices if read_gradient_from_file(index) is not None)\n    print('current y1 =',y1,'total_gradient left=', total_gradient, 'flag = ', flag)\n    # exit(1)\n    return total_gradient\n\ndef calculate_gradient_summation_left(y1, z1, flag):\n    y1 += 1\n    if y1 > 150:\n        return 0\n\n    if flag == True:\n       coordinates = [\n            (0, y1, z1), (1, y1+1, z1), (2, y1 + 1, z1), (3, y1 + 2, z1), (4, y1 + 2, z1),\n            (5, y1 + 2, z1), (6, y1 + 3, z1), (7, y1 + 3, z1), (8, y1 + 4, z1), (9, y1 + 4, z1),\n            (10, y1 + 4, z1), (11, y1 + 5, z1), (12, y1 + 5, z1), (13, y1 + 6, z1), (14, y1 + 6, z1),\n            (15, y1 + 6, z1), (16, y1 + 7, z1), (17, y1 + 7, z1), (18, y1 + 8, z1), (19, y1 + 8, z1),\n            (20, y1 + 8, z1), (21, y1 + 9, z1), (22, y1 + 9, z1), (23, y1 + 10, z1), (24, y1 + 10, z1), (25, y1+10, z1)\n        ]\n\n    else:\n        coordinates = [\n            (0, y1, z1), (1, y1-1, z1), (2, y1-1, z1), (3, y1-2, z1), (4, y1-2, z1),\n            (5, y1-2, z1), (6, y1-3, z1), (7, y1-3, z1), (8, y1-4, z1), (9, y1-4, z1),\n            (10, y1-4, z1), (11, y1-5, z1), (12, y1-5, z1), (13, y1-6, z1), (14, y1-6, z1),\n            (15, y1-6, z1), (16, y1-7, z1), (17, y1-7, z1), (18, y1-8, z1), (19, y1-8, z1),\n            (20, y1-8, z1), (21, y1-9, z1), (22, y1-9, z1), (23, y1-10, z1), (24, y1-10, z1), (25, y1-10, z1)\n        ]\n\n    indices = [coordinate_to_index(x, y, z) for x, y, z in coordinates]\n    print('index right= ', indices)\n\n    # 각 인덱스에 대해 gradient 값을 읽어와 합산합니다.\n    total_gradient = sum(read_gradient_from_file(index) for index in indices if read_gradient_from_file(index) is not None)\n    print('current y1 =',y1, 'total_gradient right=', total_gradient, 'flag = ', flag)\n    # exit(1)\n    return total_gradient\n\n\ndef comparison_gradient(y1, z1, flag):\n    # Calculate gradients from both sides\n    direction_flag = flag\n    left_gradient = calculate_gradient_summation_left(y1, z1, direction_flag)\n    right_gradient = calculate_gradient_summation_right(y1, z1, direction_flag)\n    \n    print('left gradient =', left_gradient)\n    print('right gradient =', right_gradient)\n    \n    # Compare the absolute values of the gradients\n    if abs(left_gradient) > abs(right_gradient):\n        return left_gradient\n    else:\n        return right_gradient\n\n### Part 2. Shape optimization ###\ndef update_geometry_file(coordinates, new_value):\n    # 파일을 읽고 모든 줄을 메모리에 저장\n    if rank == 0:\n        try:\n            with o",
  "Oblique_Planewave/README.md": "# Oblique_Planewave\nOblique Planewave example\n",
  "meep_python/adjoint_optimization_examples/01-Introduction.py": "# # Meep Adjoint Solver - Introduction\n# In[1]:\n\nimport meep as mp # MEEP\nimport meep.adjoint as mpa # adjoint library\nimport numpy as np # 배열\nfrom autograd import numpy as npa # 자동 미분 기능\nfrom matplotlib import pyplot as plt # matplot.pyplot을 불러오고 이를 plt로 정의 (그래프 그리는 용도)\n\nmp.verbosity(0) # verbosity flag를 지정 : C/C++ 라이브러리에 있는 전역변수에 접근가능\n                # 0: minimal output, 1: a little, 2: a lot, 3: debugging\nseed = 240 #240 으로 설정한건 큰 의미 없음\nnp.random.seed(seed) #난수 생성기 : 앞선 seed=240를 통해 해당 numpy는 유사 난수를 생성함\nSi = mp.Medium(index=3.4) #물질 정의 meep.Medium(A(eps), B(conductivity)) 기본적으로 Isotropic 한 물질을 다룸\nSiO2 = mp.Medium(index=1.44) # index=number 는 epsilon=number^2 을 의미함.\n\n# In[2]:\n\nresolution = 20\n\nSx = 6\nSy = 5\ncell_size = mp.Vector3(Sx, Sy) # 2D 6x5 :120x100 (pixcels)\n\npml_layers = [mp.PML(1.0)] # PML 두께 1로 설정\n\n# In[3]:\n\nfcen = 1 / 1.55 # 중심주파수 1/1.55\nwidth = 0.1\nfwidth = width * fcen\nsource_center = [-1, 0, 0]\nsource_size = mp.Vector3(0, 2, 0)\nkpoint = mp.Vector3(1, 0, 0)\nsrc = mp.GaussianSource(frequency=fcen, fwidth=fwidth) #source for main source (Gaussian src) 필수 설정값: 주파수 or 대역, fwidth.\n                                                      #(input: freq, width, fwidth, start time, cutoff, is integrated, wavelength)\nsource = [\n    mp.EigenModeSource( # main source\n        src, # Source : Continuous, Gaussian, Custom > 이경우는 Gaussian source\n        eig_band=1,# 1: 정의된 주파수 중에서 가장 낮은 주파수를 의미 이경우 단일 주파수 이므로 fcen\n        direction=mp.NO_DIRECTION,# main source가 kpoint의 방향을 따 즉, oblique source를 쓸때 사용\n        eig_kpoint=kpoint,# (1,0,0)이므로 x방향\n        size=source_size,# source 크기 (y plane: 2)\n        center=source_center,# Sourec 중심 위치 x = -1\n    )\n]\n\n# In[4]:\n\ndesign_region_resolution = 10 # 설계영역 resolution : 4pixels (2x2)\nNx = design_region_resolution + 1 #설계공간 x grid size (11이면 지점 11개, 즉, 10칸)\nNy = design_region_resolution + 1 #설계공간 y grid\n\ndesign_variables = mp.MaterialGrid(mp.Vector3(Nx, Ny), SiO2, Si, grid_type=\"U_MEAN\")# U_MEAN은 곂치는 그리드값을 평균 처리함을 의미\ndesign_region = mpa.DesignRegion(\n    design_variables, volume=mp.Volume(center=mp.Vector3(), size=mp.Vector3(1, 1, 0))\n)#설계 영역 : 전체영역 중앙의 1x1 공간 (구성물질은 SiO2와 Si)\n\n\ngeometry = [\n    mp.Block(\n        center=mp.Vector3(x=-Sx / 4), material=Si, size=mp.Vector3(Sx / 2, 0.5, 0)\n    ),  # horizontal waveguide\n    mp.Block(\n        center=mp.Vector3(y=Sy / 4), material=Si, size=mp.Vector3(0.5, Sy / 2, 0)\n    ),  # vertical waveguide\n    mp.Block(\n        center=design_region.center, size=design_region.size, material=design_variables\n    ),  # design region\n    # mp.Block(center=design_region.center, size=design_region.size, material=design_variables,\n    #        e1=mp.Vector3(x=-1).rotate(mp.Vector3(z=1), np.pi/2), e2=mp.Vector3(y=1).rotate(mp.Vector3(z=1), np.pi/2))\n]\n\n# In[5]:\n\nsim = mp.Simulation(\n    cell_size=cell_size,# 시뮬레이션 공간\n    boundary_layers=pml_layers,# 사방에 pml\n    geometry=geometry,# Waveguide\n    sources=source,# Gaussian source\n    eps_averaging=False,# 물질을 이미 선언했기에 사용하지 않는 기능\n    resolution=resolution,# 20\n)# simulation\n\n# In[6]:\n\nTE0 = mpa.EigenmodeCoefficient(\n    sim, mp.Volume(center=mp.Vector3(0, 1, 0), size=mp.Vector3(x=2)), mode=1\n)# 계산할 지점 및 값 정의 : waveguide의 윗부분 with mode1\nob_list = [TE0]\n\n# In[7]:\n\ndef J(alpha):\n    return npa.abs(alpha) ** 2 # FoM :J = d |alpha|^2 > autograd(npa) 를 이용하여 미분\n\n# In[8]:\n\nopt = mpa.OptimizationProblem(\n    simulation=sim,# simulation\n    objective_functions=J,# FoM\n    objective_arguments=ob_list,# Input for FoM\n    design_regions=[design_region],\n    fcen=fcen,\n    df=0,\n    nf=1,# single frequency\n)\n\n# In[9]:\n\nx0 = np.random.rand(Nx * Ny)# 1D 121 array of ramdom value\nopt.update_design([x0])# 121개의 난수로 설계공간의 모든 point update (eps)\n\n# In[10]:\n\nopt.plot2D(True, frequency=1 / 1.55)# True 로 하면, 저장되어있는 field 삭제\nplt.show() # Simulation 2D field monitor\n\n# In[11]:\n\nf0, dJ_du = opt()# f0= current state of FoM, dJ_du=gradient of FoM\n\n# In[12]:\n\nplt.figure()\nplt.imshow(np.rot90(dJ_du.reshape(Nx, Ny)))# gradient map\n\n# In[13]:\n\ndb = 1e-3 # The step size of finite difference\nchoose = 10 # number of gradients\ng_discrete, idx = opt.calculate_fd_gradient(num_gradients=choose, db=db)# 1~121(설계영역 픽셀 수)중 비복원추출한 10개의 값 (idx, array)과\n                                                        # 해당 위치에서의 10개의 gradient array (g_discrete) 출력\n\n# In[14]:\n\n(m, b) = np.polyfit(np.squeeze(g_discrete), dJ_du[idx], 1)# 앞서 구한 gradient array들을 하나의 array로 합치고(squeeze),\n                                                        # x: finite difference gradient, y: adjoint gradient 상의 1차 함수로 fitting\n# In[15]:\n\nmin_g = np.min(g_discrete)\nmax_g = np.max(g_discrete)\n\nfig = plt.figure(figsize=(12, 6))\n\nplt.subplot(1, 2, 1)\nplt.plot([min_g, max_g], [min_g, max_g], label=\"y=x comparison\")\nplt.plot([min_g, max_g], [m * min_g + b, m * max_g + b], \"--\", label=\"Best fit\")\nplt.plot(g_discrete, dJ_du[idx], \"o\", label=\"Adjoint comparison\")\nplt.xlabel(\"Finite Difference Gradient\")\nplt.ylabel(\"Adjoint Gradient\")\nplt.legend()\nplt.grid(True)\nplt.axis(\"square\")  # FD"
}